# Signal.Engine — Lightweight Deploy Requirements (API Only)
#
# Use this for Koyeb / PaaS deployments where build size/time is limited.
# Excludes training-only packages: torch, gudhi, optuna, matplotlib.
# The RL Expert uses pure numpy inference (no PyTorch needed at runtime).

# Core API
fastapi>=0.109.0,<1.0.0
uvicorn>=0.27.0,<1.0.0
python-multipart>=0.0.6,<1.0.0
pydantic-settings>=2.1.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
websockets>=12.0,<14.0

# Data & Analysis (runtime)
pandas>=2.1.0,<3.0.0
numpy>=1.26.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0
scipy>=1.11.0,<2.0.0
ta>=0.11.0,<1.0.0
yfinance>=0.2.31,<1.0.0

# Data Providers
alpaca-py>=0.21.0,<1.0.0
requests>=2.31.0,<3.0.0

# Parsing
lxml>=5.0.0,<6.0.0

# Testing
pytest>=7.4.0,<9.0.0
httpx>=0.25.0,<1.0.0
